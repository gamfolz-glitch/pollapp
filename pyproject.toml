[tool.ruff]
line-length = 100
target-version = "py312"
extend-exclude = [
  ".venv",
  "**/migrations/**",
  "staticfiles",
]

[tool.ruff.lint]
select = ["E", "F", "I", "UP", "B"]

[tool.ruff.lint.isort]
known-first-party = ["config", "polls"]
